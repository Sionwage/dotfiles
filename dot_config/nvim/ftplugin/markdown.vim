" ~/.config/nvim/ftplugin/markdown.vim
setlocal expandtab        " Use spaces instead of literal tabs
setlocal tabstop=4        " How many spaces a TAB character looks like
setlocal shiftwidth=4     " Indent/outdent with >> << uses 4 spaces
setlocal softtabstop=4    " How many spaces a press of TAB inserts
